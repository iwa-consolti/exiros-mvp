# android — App de operadores (Kotlin nativo)

App de acceso libre (sin login) para operadores (ADR-004). Rastreo en 2º plano:
FusedLocation + ActivityRecognition + Foreground Service + Room + WorkManager.

Se materializa en el **Bloque 1.4** (proyecto Kotlin vacío que compila en emulador).
