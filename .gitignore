# === Secretos / entorno ===
.env
.env.*
!.env.example
*.pem
*.key

# === Node (backend NestJS / web React) ===
node_modules/
dist/
build/
coverage/
*.log
npm-debug.log*
.npm/
.eslintcache

# === Android / Kotlin / Gradle ===
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.cxx/
*.apk
*.aab
*.keystore
!debug.keystore
android/keystore.properties

# === Secretos de despliegue (generados local, NUNCA al repo) ===
infra/secrets.local.env
*.local.env

# === Bundle de llaves exportadas (keystore + .env + props; NUNCA al repo) ===
# Idealmente vive FUERA del repo; esto es la red de seguridad por si queda dentro.
KEYS-EXPORTED/
keystore.properties

# === Prisma ===
# (migraciones SÍ se versionan; solo se ignora el cliente generado si cae en repo)
**/generated/

# === IDE ===
.idea/
.vscode/
*.iml

# === SO ===
.DS_Store
Thumbs.db
desktop.ini

# === Subidas locales (fotos MVP en disco del backend) ===
uploads/

# === Artefactos de verificación (Playwright MCP) ===
.playwright-mcp/
